# 🧪 تست خودکار قبل از استقرار

قبل از هر بار آپلود روی سرور، این تست را اجرا کنید تا مطمئن شوید همه‌چیز کار می‌کند.

## نصب (فقط یک‌بار)

```bash
npm install jsdom
```

## اجرا

```bash
node test-subpage.js
```

## چه چیزی تست می‌شود؟

فایل `sub-page-index.html` در یک مرورگر شبیه‌سازی‌شده (jsdom) واقعاً اجرا می‌شود و بررسی می‌شود:

- سوالات متداول رندر می‌شوند
- اپلیکیشن‌ها رندر می‌شوند (هر تعداد)
- نشان «پیشنهادی» و دکمه‌ی «افزودن یک‌کلیک»
- نام برند، عنوان صفحه
- رنگ سفارشی، CSS سفارشی، پاورقی
- لینک‌های پشتیبانی و کانال
- ویدیوهای آموزشی
- تعویض تب پلتفرم
- تعویض زبان
- بدون خطای جاوااسکریپت

## خروجی موفق

```
نتیجه: 20/21 تست پاس شد
```

توجه: خطای `HTTP 404` مربوط به `fetchLiveSubscriptionData` طبیعی است — چون در محیط تست، پنل 3x-ui واقعی وجود ندارد. این خطا داخل `try/catch` مدیریت می‌شود و صفحه را نمی‌شکند.

## اگر تستی رد شد

خروجی دقیقاً می‌گوید کدام بخش مشکل دارد و چه مقداری انتظار می‌رفت — قبل از آپلود روی سرور آن را رفع کنید.

## تست‌های ربات

```bash
cd bot
python3 test_bot.py     # ۷۱ تست واحد
python3 test_flow.py    # ۳۸ تست جریان خرید
python3 test_admin.py   # ۳۴ تست پنل مدیریت
```

`test_admin.py` مسیر واقعی را دنبال می‌کند — از فشردن دکمه تا نتیجه در دیتابیس.
تست مستقیم توابع کافی نیست: دو باگ (تشخیص ادمین و متغیر تعریف‌نشده) فقط با
همین روش پیدا شدند.

## تست رندر پنل

```bash
cd frontend && npm install && npm run build && cd ..
node test-panel-render.js
```

باندل واقعی را در jsdom اجرا می‌کند. اگر پنل بالا نیاید یا خطای
جاوااسکریپت بدهد، تست ناموفق می‌شود.

**چرا لازم است:** `npm run build` یک import گمشده را نمی‌گیرد —
جاوااسکریپت معتبر است تا وقتی اجرا شود. نسخه‌ی ۲.۴.۱ دقیقاً به همین
دلیل صفحه‌ی سفید می‌داد.

## تست اجرای پنل

```bash
cd frontend && npm run build && cd ..
node test-panel-runtime.js
```

بیلد موفق تضمین نمی‌کند که پنل در مرورگر کار کند — یک import حذف‌شده
باندل سالمی می‌سازد که هنگام اجرا کرش می‌کند و صفحه سفید می‌شود.
این تست باندل را واقعاً در jsdom اجرا می‌کند و رندر شدن را بررسی می‌کند.
